export interface gradingCardDataType {
  id: number;
  title: string;
  subTitle: string;
  quantity: number;
  minPrice: number;
  minPriceQuantity: number;
  maxPrice: number;
  maxPriceQuantity: number;
  lastPrice: number;
}
